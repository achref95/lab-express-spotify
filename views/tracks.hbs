{{>nav}}
{{#each item}}
<figure>
        <figcaption>{{track_number}}.  {{name}}</figcaption>
    <audio
        controls
        src="{{preview_url}}">
            <a href="{{href}}">
                Download audio
            </a>
    </audio>
</figure>

{{/each}}